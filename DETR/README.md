# DETR

Run the notebook from the repository root with the standard executor:

```bash
uv run python tools/execute_notebooks.py --model DETR --allow-large-downloads
```

To build the local notebook environment for interactive use:

```bash
cd DETR
uv sync
```

Then select `DETR/.venv/bin/python` as the notebook kernel.
