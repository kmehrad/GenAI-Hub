# BiRefNet

Run the notebook from the repository root with the standard executor:

```bash
uv run python tools/execute_notebooks.py --model BiRefNet --allow-large-downloads
```

To build the local notebook environment for interactive use:

```bash
cd BiRefNet
uv sync
```

Then select `BiRefNet/.venv/bin/python` as the notebook kernel.
